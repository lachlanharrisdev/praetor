# pt start

Initialise a new engagement directory, with Praetor metadata and user template files, and point to the absolute path on the system.

  `pt start <name>`

## Examples

```sh
# create a new engagement
$ pt start "client_01"
```
